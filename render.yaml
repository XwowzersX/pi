services:
  - type: web
    name: pi-stream-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --worker-class gevent --workers 1 --threads 1 --bind 0.0.0.0:$PORT
